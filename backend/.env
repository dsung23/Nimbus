# Environment Variables Setup Guide

## Creating Your .env File

1. Create a `.env` file in the backend root directory
2. Copy the following template and fill in your actual values:

```env
# Server Configuration
PORT=4000
NODE_ENV=development

# Supabase Configuration
SUPABASE_URL=https://mbpwyjiktgszacxsqyzo.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1icHd5amlrdGdzemFjeHNxeXpvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTA0NjM1MTcsImV4cCI6MjA2NjAzOTUxN30.XOs5fC-jl623OHb8eDroE0T4q-hcJzB-c8eRVTQLLt4
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1icHd5amlrdGdzemFjeHNxeXpvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTA0NjM1MTcsImV4cCI6MjA2NjAzOTUxN30.XOs5fC-jl623OHb8eDroE0T4q-hcJzB-c8eRVTQLLt4

# Plaid API Configuration
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret_key
PLAID_ENV=sandbox

TELLER_APP_ID=your-application-id    
TELLER_ENVIRONMENT=sandbox              


TELLER_CERTIFICATE_PATH=./certs/certificate.pem
TELLER_PRIVATE_KEY_PATH=./certs/private_key.pem
TELLER_CERTIFICATE_PASSPHRASE=           

TELLER_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxx

TELLER_SANDBOX_ACCESS_TOKEN=sandbox-access-token-abcd1234


# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_random
JWT_EXPIRES_IN=7d

# LLM API Configuration (Choose one)
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4

# Redis Configuration (for caching)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=your_redis_password

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:19006

# Security
BCRYPT_ROUNDS=12
```

## Getting Your API Keys

### Plaid Setup
1. Go to [plaid.com](https://plaid.com) and create a developer account
2. Navigate to Dashboard > API Keys
3. Copy the following values:
   - Client ID → `PLAID_CLIENT_ID`
   - Sandbox Secret → `PLAID_SECRET` (use Development Secret for production)
4. Set `PLAID_ENV=sandbox` for development

### OpenAI Setup (for LLM)
1. Go to [platform.openai.com](https://platform.openai.com)
2. Create an account and add billing information
3. Navigate to API Keys
4. Create a new API key → `OPENAI_API_KEY`
5. Set `OPENAI_MODEL=gpt-4` or `gpt-3.5-turbo`

### JWT Secret
Generate a secure random string for `JWT_SECRET`:
```bash
# Option 1: Using Node.js
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# Option 2: Using OpenSSL
openssl rand -hex 64
```

## Security Notes

- **Never commit your .env file to version control**
- **Keep your API keys secure and rotate them regularly**
- **Use different keys for development and production**
- **The .env file is already in .gitignore**

## Testing Your Setup

After creating your .env file, you can test the configuration by running:
```bash
npm run dev
```

The server should start without errors if all environment variables are properly configured. 